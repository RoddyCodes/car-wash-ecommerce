import React from 'react';
import { Container, Typography } from '@mui/material';

export default function Checkout() {
  return (
    <Container sx={{ mt: 4 }}>
      <Typography variant="h4">Checkout</Typography>
      <Typography>Checkout functionality coming soon!</Typography>
    </Container>
  );
}
